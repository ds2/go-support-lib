buildscript {
    repositories {
        mavenLocal()
        jcenter()
    }
    dependencies {
        classpath "ds2.gradle.plugins.golang:gradle-go-plugin:0.1.9-SNAPSHOT"
    }
}

apply plugin: "ds2.gradle.plugins.golang"

// plugins {
//   id "ds2.gradle.plugins.golang" version "0.1.9-SNAPSHOT"
// }

repositories {
    mavenLocal()
    jcenter()
}

go {
    modules Arrays.asList('gitlab.com/ds_2/go_support-lib')
    //modules ['github.com/ds2/golang/sysinfo']
    buildMode='archive'
    verbose true
    //outputName='helloWorld'
    //target = BuildModes.ARCHIVE
}
