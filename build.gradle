plugins {
  id "ds2.gradle.plugins.golang" version "0.1.8"
}

group = 'ds2.golang'
version ='0.0.1-SNAPSHOT'

repositories {
    mavenLocal()
    jcenter()
}

go {
    modules Arrays.asList('github.com/ds2/golang/sysinfo')
    buildMode BuildMode.ARCHIVE
}
